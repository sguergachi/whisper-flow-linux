[project]
name = "whisper-flow"
version = "0.1.0"
description = "AI-powered voice-to-text flow application with context-aware processing"
readme = "README.md"
authors = [
    { name = "sapountzis", email = "sapountzis.andreas@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
    "requests>=2.31.0",
    "pyaudio>=0.2.11",
    "webrtcvad>=2.0.10",
    "typer>=0.9.0",
    # webrtcvad imports pkg_resources at module load, which setuptools 81
    # removed. Until webrtcvad stops doing that, the ceiling has to stay.
    "setuptools>=69.0.0,<81",
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "numpy>=1.24.0",
    "pystray>=0.19.0",
    "pillow>=10.0.0",
    "openai>=1.93.0",
    # Reading the keyboard on Linux. Imported by hotkey_evdev, and previously
    # undeclared, so a clean install failed at first hotkey use.
    "evdev>=1.6.1; sys_platform == 'linux'",
    # X11 fallback listener. Not used on the Wayland path and has no purpose
    # on Windows, where the native listener handles hotkeys.
    "pynput>=1.7.6; sys_platform != 'win32'",
    "pywin32>=306; sys_platform == 'win32'",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "isort>=5.12.0",
    "mypy>=1.0.0",
    "ruff>=0.1.0",
]

[project.gui-scripts]
whisper-flow-tray = "whisper_flow.daemon:main"

[project.scripts]
whisper-flow = "whisper_flow.cli:main_entry"
whisper-flow-dictation = "whisper_flow.cli:dictation_entry"
whisper-flow-daemon = "whisper_flow.daemon:main"

[project.urls]
Homepage = "https://github.com/sapountzis/whisper-flow"
Repository = "https://github.com/sapountzis/whisper-flow"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.black]
line-length = 88
target-version = ['py312']

[tool.isort]
profile = "black"

[tool.ruff]
target-version = "py312"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
    "--disable-warnings",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
